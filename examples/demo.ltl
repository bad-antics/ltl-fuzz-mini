// examples/demo.ltl — runnable demo for ltl_fuzz_mini

import std::io
use ltl_fuzz_mini as lib

fn main() {
    io::println("ltl_fuzz_mini demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}